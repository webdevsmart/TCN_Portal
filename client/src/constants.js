const USER_ROLE = [
    'Client', 'Admin'
];

const STATUS_ACTIVE = 'Active'; 
const STATUS_DEACTIVE = 'Inactive'; 

const RELEASE_TYPE = [
    'Spiral',
    'Conveyor',
    'Claw',
    'Other'
]

const LENGTH_UNIT = [
    'cms', 'mms'
];

const WEIGHT_UNIT = [
    'gms', 'kgs'
];

const PRICE_UNIT = [
    'AUD$', 'USD$'
];

const YES = 'Yes'; 
const NO = 'No'; 

const IMAGE_UPLOAD_URL = '/uploads/images/';

module.exports = { 
    USER_ROLE,
    STATUS_ACTIVE,
    STATUS_DEACTIVE,
    RELEASE_TYPE,
    YES,
    NO,
    IMAGE_UPLOAD_URL,
    LENGTH_UNIT,
    WEIGHT_UNIT,
    PRICE_UNIT
};
